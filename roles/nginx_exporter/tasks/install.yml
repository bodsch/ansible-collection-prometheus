---

- name: user and group handling
  when:
    - not running_in_check_mode
    - nginx_exporter_system_user != "root"
  block:
    - name: create nginx_exporter group
      ansible.builtin.group:
        name: "{{ nginx_exporter_system_group }}"
        state: present
        system: true

    - name: create nginx_exporter user
      ansible.builtin.user:
        name: "{{ nginx_exporter_system_user }}"
        groups: "{{ nginx_exporter_system_group }}"
        append: true
        shell: /usr/sbin/nologin
        system: true
        createhome: false
        home: /nonexistent

- name: create install directory
  when:
    - not running_in_check_mode
  ansible.builtin.file:
    path: "{{ nginx_exporter_install_path }}"
    state: directory
    owner: "{{ nginx_exporter_system_user }}"
    group: "{{ nginx_exporter_system_group }}"
    mode: "0755"

- name: deploy and activate nginx_exporter version {{ nginx_exporter_version }}
  when:
    - not running_in_check_mode
  bodsch.core.deploy_and_activate:
    src_dir: "{{ nginx_exporter_local_tmp_directory }}"
    install_dir: "{{ nginx_exporter_install_path }}"
    link_dir: "/usr/bin"
    remote_src: "{{ ('true' if nginx_exporter_direct_download else 'false') | bool }}"
    owner: "{{ nginx_exporter_system_user }}"
    group: "{{ nginx_exporter_system_group }}"
    mode: "0755"
    items:
      - name: "{{ nginx_exporter_release.binary }}"
        capability: "cap_net_raw+ep"
  notify:
    - restart nginx exporter

- name: systemd
  when:
    - not running_in_check_mode
    - ansible_facts.service_mgr | lower == "systemd"
  block:
    - name: create nginx_exporter run configuration
      ansible.builtin.template:
        src: "default/nginx_exporter.j2"
        dest: "{{ nginx_exporter_defaults_directory }}/nginx_exporter"
        force: true
        owner: root
        group: "{{ nginx_exporter_system_group }}"
        mode: "0640"
      notify:
        - restart nginx exporter

    - name: create systemd service unit
      ansible.builtin.template:
        src: "init/systemd/nginx_exporter.service.j2"
        dest: "{{ systemd_lib_directory }}/nginx_exporter.service"
        owner: root
        group: root
        mode: "0644"
      notify:
        - daemon-reload
        - restart nginx exporter

- name: openrc
  when:
    - not running_in_check_mode
    - ansible_facts.service_mgr | lower == "openrc"
  block:
    - name: create openrc service configuration
      ansible.builtin.template:
        src: "init/openrc/conf.d/nginx_exporter.j2"
        dest: "/etc/conf.d/nginx_exporter"
        owner: root
        group: root
        mode: "0644"

    - name: create openrc init configuration
      ansible.builtin.template:
        src: "init/openrc/init.d/nginx_exporter.j2"
        dest: "/etc/init.d/nginx_exporter"
        owner: root
        group: root
        mode: "0750"

- name: create custom fact file
  when:
    - not running_in_check_mode
  bodsch.core.facts:
    name: nginx_exporter
    facts:
      version: "{{ nginx_exporter_version }}"
