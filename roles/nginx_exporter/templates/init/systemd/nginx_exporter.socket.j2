#jinja2: trim_blocks: True, lstrip_blocks: True
{{ ansible_managed | comment }}

[Unit]
Description       = NGINX Prometheus Exporter

[Socket]
ListenStream      = 9113

[Install]
Alias             = nginx-exporter.socket
WantedBy          = sockets.target
