---

- name: user and group handling
  when:
    - not running_in_check_mode
    - ssl_exporter_system_user != "root"
  block:
    - name: create ssl_exporter group
      ansible.builtin.group:
        name: "{{ ssl_exporter_system_group }}"
        state: present
        system: true

    - name: create ssl_exporter user
      ansible.builtin.user:
        name: "{{ ssl_exporter_system_user }}"
        groups: "{{ ssl_exporter_system_group }}"
        append: true
        shell: /usr/sbin/nologin
        system: true
        createhome: false
        home: /nonexistent

- name: create install directory
  when:
    - not running_in_check_mode
  ansible.builtin.file:
    path: "{{ ssl_exporter_install_path }}"
    state: directory
    owner: "{{ ssl_exporter_system_user }}"
    group: "{{ ssl_exporter_system_group }}"
    mode: "0755"

- name: deploy and activate ssl_exporter version {{ ssl_exporter_version }}
  when:
    - not running_in_check_mode
  bodsch.core.deploy_and_activate:
    src_dir: "{{ ssl_exporter_local_tmp_directory }}"
    install_dir: "{{ ssl_exporter_install_path }}"
    link_dir: "/usr/bin"
    remote_src: "{{ ('true' if ssl_exporter_direct_download else 'false') | bool }}"
    owner: "{{ ssl_exporter_system_user }}"
    group: "{{ ssl_exporter_system_group }}"
    mode: "0755"
    items:
      - name: "{{ ssl_exporter_release.binary }}"
        capability: "cap_net_raw+ep"
  notify:
    - restart ssl exporter

- name: systemd
  when:
    - not running_in_check_mode
    - ansible_facts.service_mgr | lower == "systemd"
  block:
    - name: create ssl_exporter run configuration
      ansible.builtin.template:
        src: "default/ssl_exporter.j2"
        dest: "{{ ssl_exporter_defaults_directory }}/ssl_exporter"
        force: true
        owner: root
        group: "{{ ssl_exporter_system_group }}"
        mode: "0640"
      notify:
        - restart ssl exporter

    - name: create systemd service unit
      ansible.builtin.template:
        src: "init/systemd/ssl_exporter.service.j2"
        dest: "{{ systemd_lib_directory }}/ssl_exporter.service"
        owner: root
        group: root
        mode: "0644"
      notify:
        - daemon-reload
        - restart ssl exporter

- name: openrc
  when:
    - not running_in_check_mode
    - ansible_facts.service_mgr | lower == "openrc"
  block:
    - name: create openrc service configuration
      ansible.builtin.template:
        src: "init/openrc/conf.d/ssl_exporter.j2"
        dest: "/etc/conf.d/ssl_exporter"
        owner: root
        group: root
        mode: "0644"

    - name: create openrc init configuration
      ansible.builtin.template:
        src: "init/openrc/init.d/ssl_exporter.j2"
        dest: "/etc/init.d/ssl_exporter"
        owner: root
        group: root
        mode: "0750"

- name: create custom fact file
  when:
    - not running_in_check_mode
  bodsch.core.facts:
    name: ssl_exporter
    facts:
      version: "{{ ssl_exporter_version }}"

...
