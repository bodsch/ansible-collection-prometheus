---

- name: user and group handling
  when:
    - not running_in_check_mode
    - bind_exporter_system_user != "root"
  block:
    - name: create bind_exporter group
      ansible.builtin.group:
        name: "{{ bind_exporter_system_group }}"
        state: present
        system: true

    - name: create bind_exporter user
      ansible.builtin.user:
        name: "{{ bind_exporter_system_user }}"
        groups: "{{ bind_exporter_system_group }}"
        append: true
        shell: /usr/sbin/nologin
        system: true
        createhome: false
        home: /nonexistent

- name: create install directory
  when:
    - not running_in_check_mode
  ansible.builtin.file:
    path: "{{ bind_exporter_install_path }}"
    state: directory
    owner: "{{ bind_exporter_system_user }}"
    group: "{{ bind_exporter_system_group }}"
    mode: "0755"

- name: deploy and activate bind_exporter version {{ bind_exporter_version }}
  when:
    - not running_in_check_mode
  bodsch.core.deploy_and_activate:
    src_dir: "{{ bind_exporter_local_tmp_directory }}"
    install_dir: "{{ bind_exporter_install_path }}"
    link_dir: "/usr/bin"
    remote_src: "{{ ('true' if bind_exporter_direct_download else 'false') | bool }}"
    owner: "{{ bind_exporter_system_user }}"
    group: "{{ bind_exporter_system_group }}"
    mode: "0755"
    items:
      - name: "{{ bind_exporter_release.binary }}"
        capability: "cap_net_raw+ep"
  notify:
    - restart bind exporter

- name: systemd
  when:
    - not running_in_check_mode
    - ansible_facts.service_mgr | lower == "systemd"
  block:
    - name: create bind_exporter run configuration
      ansible.builtin.template:
        src: "default/bind_exporter.j2"
        dest: "{{ bind_exporter_defaults_directory }}/bind_exporter"
        force: true
        owner: root
        group: "{{ bind_exporter_system_group }}"
        mode: "0640"
      notify:
        - restart bind exporter

    - name: create systemd service unit
      ansible.builtin.template:
        src: "init/systemd/bind_exporter.service.j2"
        dest: "{{ systemd_lib_directory }}/bind_exporter.service"
        owner: root
        group: root
        mode: "0644"
      notify:
        - daemon-reload
        - restart bind exporter

- name: openrc
  when:
    - not running_in_check_mode
    - ansible_facts.service_mgr | lower == "openrc"
  block:
    - name: create openrc service configuration
      ansible.builtin.template:
        src: "init/openrc/conf.d/bind_exporter.j2"
        dest: "/etc/conf.d/bind_exporter"
        owner: root
        group: root
        mode: "0644"

    - name: create openrc init configuration
      ansible.builtin.template:
        src: "init/openrc/init.d/bind_exporter.j2"
        dest: "/etc/init.d/bind_exporter"
        owner: root
        group: root
        mode: "0750"

- name: create custom fact file
  when:
    - not running_in_check_mode
  bodsch.core.facts:
    name: bind_exporter
    facts:
      version: "{{ bind_exporter_version }}"

...
