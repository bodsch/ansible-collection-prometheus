---

- name: user and group handling
  when:
    - not running_in_check_mode
    - redis_exporter_system_user != "root"
  block:
    - name: create redis_exporter group
      ansible.builtin.group:
        name: "{{ redis_exporter_system_group }}"
        state: present
        system: true

    - name: create redis_exporter user
      ansible.builtin.user:
        name: "{{ redis_exporter_system_user }}"
        groups: "{{ redis_exporter_system_group }}"
        append: true
        shell: /usr/sbin/nologin
        system: true
        createhome: false
        home: /nonexistent

- name: create install directory
  when:
    - not running_in_check_mode
  ansible.builtin.file:
    path: "{{ redis_exporter_install_path }}"
    state: directory
    owner: "{{ redis_exporter_system_user }}"
    group: "{{ redis_exporter_system_group }}"
    mode: "0755"

- name: deploy and activate redis_exporter version {{ redis_exporter_version }}
  when:
    - not running_in_check_mode
  bodsch.core.deploy_and_activate:
    src_dir: "{{ redis_exporter_local_tmp_directory }}"
    install_dir: "{{ redis_exporter_install_path }}"
    link_dir: "/usr/bin"
    remote_src: "{{ ('true' if redis_exporter_direct_download else 'false') | bool }}"
    owner: "{{ redis_exporter_system_user }}"
    group: "{{ redis_exporter_system_group }}"
    mode: "0755"
    items:
      - name: "{{ redis_exporter_release.binary }}"
        capability: "cap_net_raw+ep"
  notify:
    - restart redis exporter

- name: systemd
  when:
    - not running_in_check_mode
    - ansible_facts.service_mgr | lower == "systemd"
  block:
    - name: create redis_exporter run configuration
      ansible.builtin.template:
        src: "default/redis_exporter.j2"
        dest: "{{ redis_exporter_defaults_directory }}/redis_exporter"
        force: true
        owner: root
        group: "{{ redis_exporter_system_group }}"
        mode: "0640"
      notify:
        - restart redis exporter

    - name: create systemd service unit
      ansible.builtin.template:
        src: "init/systemd/redis_exporter.service.j2"
        dest: "{{ systemd_lib_directory }}/redis_exporter.service"
        owner: root
        group: root
        mode: "0644"
      notify:
        - daemon-reload
        - restart redis exporter

- name: openrc
  when:
    - not running_in_check_mode
    - ansible_facts.service_mgr | lower == "openrc"
  block:
    - name: create openrc service configuration
      ansible.builtin.template:
        src: "init/openrc/conf.d/redis_exporter.j2"
        dest: "/etc/conf.d/redis_exporter"
        owner: root
        group: root
        mode: "0644"

    - name: create openrc init configuration
      ansible.builtin.template:
        src: "init/openrc/init.d/redis_exporter.j2"
        dest: "/etc/init.d/redis_exporter"
        owner: root
        group: root
        mode: "0750"

- name: create custom fact file
  when:
    - not running_in_check_mode
  bodsch.core.facts:
    name: redis_exporter
    facts:
      version: "{{ redis_exporter_version }}"

...
