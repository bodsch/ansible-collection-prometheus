---

- name: user and group handling
  when:
    - not running_in_check_mode
    - mysql_exporter_system_user != "root"
  block:
    - name: create mysql_exporter group
      ansible.builtin.group:
        name: "{{ mysql_exporter_system_group }}"
        state: present
        system: true

    - name: create mysql_exporter user
      ansible.builtin.user:
        name: "{{ mysql_exporter_system_user }}"
        groups: "{{ mysql_exporter_system_group }}"
        append: true
        shell: /usr/sbin/nologin
        system: true
        createhome: false
        home: /nonexistent

- name: create install directory
  when:
    - not running_in_check_mode
  ansible.builtin.file:
    path: "{{ mysql_exporter_install_path }}"
    state: directory
    owner: "{{ mysql_exporter_system_user }}"
    group: "{{ mysql_exporter_system_group }}"
    mode: "0755"

- name: deploy and activate mysql_exporter version {{ mysql_exporter_version }}
  when:
    - not running_in_check_mode
  bodsch.core.deploy_and_activate:
    src_dir: "{{ mysql_exporter_local_tmp_directory }}"
    install_dir: "{{ mysql_exporter_install_path }}"
    link_dir: "/usr/bin"
    remote_src: "{{ ('true' if mysql_exporter_direct_download else 'false') | bool }}"
    owner: "{{ mysql_exporter_system_user }}"
    group: "{{ mysql_exporter_system_group }}"
    mode: "0755"
    items:
      - name: "{{ mysql_exporter_release.binary }}"
        capability: "cap_net_raw+ep"
  notify:
    - restart mysql exporter

- name: systemd
  when:
    - not running_in_check_mode
    - ansible_facts.service_mgr | lower == "systemd"
  block:
    - name: create mysql_exporter run configuration
      ansible.builtin.template:
        src: "default/mysql_exporter.j2"
        dest: "{{ mysql_exporter_defaults_directory }}/mysql_exporter"
        force: true
        owner: root
        group: "{{ mysql_exporter_system_group }}"
        mode: "0640"
      notify:
        - restart mysql exporter

    - name: create systemd service unit
      ansible.builtin.template:
        src: "init/systemd/mysql_exporter.service.j2"
        dest: "{{ systemd_lib_directory }}/mysql_exporter.service"
        owner: root
        group: root
        mode: "0644"
      notify:
        - daemon-reload
        - restart mysql exporter

- name: openrc
  when:
    - not running_in_check_mode
    - ansible_facts.service_mgr | lower == "openrc"
  block:
    - name: create openrc service configuration
      ansible.builtin.template:
        src: "init/openrc/conf.d/mysql_exporter.j2"
        dest: "/etc/conf.d/mysql_exporter"
        owner: root
        group: root
        mode: "0644"

    - name: create openrc init configuration
      ansible.builtin.template:
        src: "init/openrc/init.d/mysql_exporter.j2"
        dest: "/etc/init.d/mysql_exporter"
        owner: root
        group: root
        mode: "0750"

- name: create custom fact file
  when:
    - not running_in_check_mode
  bodsch.core.facts:
    name: mysql_exporter
    facts:
      version: "{{ mysql_exporter_version }}"

...
