---

- name: user and group handling
  when:
    - not running_in_check_mode
    - json_exporter_system_user != "root"
  block:
    - name: create json_exporter group
      ansible.builtin.group:
        name: "{{ json_exporter_system_group }}"
        state: present
        system: true

    - name: create json_exporter user
      ansible.builtin.user:
        name: "{{ json_exporter_system_user }}"
        groups: "{{ json_exporter_system_group }}"
        append: true
        shell: /usr/sbin/nologin
        system: true
        createhome: false
        home: /nonexistent

- name: create install directory
  when:
    - not running_in_check_mode
  ansible.builtin.file:
    path: "{{ json_exporter_install_path }}"
    state: directory
    owner: "{{ json_exporter_system_user }}"
    group: "{{ json_exporter_system_group }}"
    mode: "0755"

- name: deploy and activate json_exporter version {{ json_exporter_version }}
  when:
    - not running_in_check_mode
  bodsch.core.deploy_and_activate:
    src_dir: "{{ json_exporter_local_tmp_directory }}"
    install_dir: "{{ json_exporter_install_path }}"
    link_dir: "/usr/bin"
    remote_src: "{{ ('true' if json_exporter_direct_download else 'false') | bool }}"
    owner: "{{ json_exporter_system_user }}"
    group: "{{ json_exporter_system_group }}"
    mode: "0755"
    items:
      - name: "{{ json_exporter_release.binary }}"
        capability: "cap_net_raw+ep"
  notify:
    - restart json exporter

- name: systemd
  when:
    - not running_in_check_mode
    - ansible_facts.service_mgr | lower == "systemd"
  block:
    - name: create json_exporter run configuration
      ansible.builtin.template:
        src: "default/json_exporter.j2"
        dest: "{{ json_exporter_defaults_directory }}/json_exporter"
        force: true
        owner: root
        group: "{{ json_exporter_system_group }}"
        mode: "0640"
      notify:
        - restart json exporter

    - name: create systemd service unit
      ansible.builtin.template:
        src: "init/systemd/json_exporter.service.j2"
        dest: "{{ systemd_lib_directory }}/json_exporter.service"
        owner: root
        group: root
        mode: "0644"
      notify:
        - daemon-reload
        - restart json exporter

- name: openrc
  when:
    - not running_in_check_mode
    - ansible_facts.service_mgr | lower == "openrc"
  block:
    - name: create openrc service configuration
      ansible.builtin.template:
        src: "init/openrc/conf.d/json_exporter.j2"
        dest: "/etc/conf.d/json_exporter"
        owner: root
        group: root
        mode: "0644"

    - name: create openrc init configuration
      ansible.builtin.template:
        src: "init/openrc/init.d/json_exporter.j2"
        dest: "/etc/init.d/json_exporter"
        owner: root
        group: root
        mode: "0750"

- name: create custom fact file
  when:
    - not running_in_check_mode
  bodsch.core.facts:
    name: json_exporter
    facts:
      version: "{{ json_exporter_version }}"

...
