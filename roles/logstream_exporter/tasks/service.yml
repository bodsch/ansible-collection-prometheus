---

- name: flush handlers
  ansible.builtin.meta: flush_handlers

- name: ensure logstream_exporter is enabled on boot
  become: true
  when:
    - not running_in_check_mode
  ansible.builtin.service:
    name: logstream_exporter
    enabled: true
    state: started

...
